export interface latLongType{
    lat:Number,
    long:Number,
}
export interface CityType{ 
    id: number,
    title: string,
    slug: string,
    province_id:number,
    latitude: number,
    longitude: number,
 }